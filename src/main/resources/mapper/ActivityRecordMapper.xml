<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secondclass.mapper.ActivityRecordMapper">

    <insert id="insert">
        INSERT INTO activity_record (activity_id, student_id, enroll_time, sign_status)
        VALUES (#{activityId}, #{studentId}, NOW(), 0)
    </insert>

    <select id="countByActivityId" resultType="int">
        SELECT COUNT(*) FROM activity_record WHERE activity_id = #{activityId}
    </select>

    <select id="checkEnrolled" resultType="int">
        SELECT COUNT(*) FROM activity_record
        WHERE activity_id = #{activityId} AND student_id = #{studentId}
    </select>

</mapper>