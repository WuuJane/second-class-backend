<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secondclass.mapper.SysDictMapper">

    <select id="selectByType" resultType="com.secondclass.entity.SysDict">
        SELECT * FROM sys_dict
        WHERE dict_type = #{dictType} AND status = 1
        ORDER BY sort_order ASC
    </select>

    <select id="checkValueExist" resultType="com.secondclass.entity.SysDict">
        SELECT * FROM sys_dict
        WHERE dict_type = #{dictType} AND dict_value = #{dictValue} LIMIT 1
    </select>

    <insert id="insertDict" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_dict (
            dict_type, dict_label, dict_value, sort_order, status, create_time
        ) VALUES (
                     #{dictType}, #{dictLabel}, #{dictValue}, #{sortOrder}, #{status}, NOW()
                 )
    </insert>

    <update id="updateDict" parameterType="com.secondclass.entity.SysDict">
        UPDATE sys_dict
        <set>
            <if test="dictLabel != null and dictLabel != ''">dict_label = #{dictLabel},</if>
            <if test="dictValue != null">dict_value = #{dictValue},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE sys_dict SET status = #{status} WHERE id = #{id}
    </update>

</mapper>